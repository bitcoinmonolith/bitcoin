{
	"name": "Deno",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"postCreateCommand": "deno task cache",

	// Configure tool-specific properties.
	"customizations": {
		"vscode": {
			"extensions": [
				"justjavac.vscode-deno-extensionpack",
				"Prisma.prisma"
			]
		}
	}
}
